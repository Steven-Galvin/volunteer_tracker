<h1>Your Current Volunteers</h1>
<% if @volunteers.any? %>
  <h3>Click on a volunteer to update:</h3>

  <ul>
    <% @volunteers.each do |volunteer| %>
      <li><a href="/volunteer/<%= volunteer.id %>"><%= volunteer.name %></a></li>
    <% end %>
  </ul>

<% else %>

  <p>There are no volunteers yet, go <a href="/">back</a> and add a volunteer to a project!</p>

<% end %>
